---
- name: "ensure bash (brew version) is installed"
  homebrew:
    name: bash
    state: latest

- name: "ensure openssh (brew version) is installed"
  homebrew:
    name: openssh
    state: latest
